/// <reference types="node" />
import { EventEmitter, Readable, Writable } from 'stream';
export declare class JSONRPCEndpoint extends EventEmitter {
    private writable;
    private readable;
    private readableByline;
    private client;
    private nextId;
    constructor(writable: Writable, readable: Readable, options?: any);
    send(method: string, message?: any): PromiseLike<any>;
    notify(method: string, message?: any): void;
}
